# Production overrides for Sokubutsu MVP (do not commit secrets here)

# Public base URL used to register Telegram Webhook at /telegram/webhook
# e.g. https://sokubutsu.denof.co.jp
ADMIN_PUBLIC_URL=https://sokubutsu.denof.co.jp

# Cloudflare Tunnel token for dockerized cloudflared (Named Tunnel)
# Obtain from Cloudflare Dashboard → Zero Trust → Tunnels → (your tunnel) → Token
# Leave empty if you run cloudflared outside of docker.
CLOUDFLARE_TUNNEL_TOKEN=

# Ensure admin port matches compose (3002)
ADMIN_PORT=3002

# Optional: enforce production flags
NODE_ENV=production
TELEGRAM_ENABLED=true
